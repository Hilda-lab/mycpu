# x86 子集 ISA（第一版）

## 目标

第一阶段聚焦 30~40 条常用指令，先保证可运行性与可测试性。

## 第一批建议

- 数据传送：MOV, PUSH, POP
- 算术逻辑：ADD, SUB, CMP, AND, OR, XOR
- 控制流：JMP, JZ, JNZ, CALL, RET, HLT

## 约束

- 先采用 x86-32 子集
- 先不实现完整分段机制
- 先用平坦内存 + 边界检查
